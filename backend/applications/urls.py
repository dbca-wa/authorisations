from django.urls import path

from .views import my_applications

urlpatterns = [
    # path("my-applications", my_applications, name="my-applications"),
    # path("<str:application_id>", display_application, name="application"),
]
